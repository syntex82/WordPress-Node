# Copy and paste this entire command on your server (one line):

cd /var/www/WordPress-Node && git pull && npm install && npx prisma migrate deploy && npx prisma generate && npm run build && cd admin && rm -rf node_modules/.vite dist && npm install && npm run build && cd .. && pm2 restart nodepress && sudo systemctl reload nginx && pm2 status

# Or run these commands one by one:

cd /var/www/WordPress-Node
git pull
npm install
npx prisma migrate deploy
npx prisma generate
npm run build
cd admin
rm -rf node_modules/.vite dist
npm install
npm run build
cd ..
pm2 restart nodepress
sudo systemctl reload nginx
pm2 status

